# Pin every dependency to an exact version in package.json (no ^ or ~ ranges),
# so installs are reproducible and version bumps are explicit, reviewable changes.
save-exact=true

# Enforce the Node/pnpm versions declared in package.json "engines".
engine-strict=true
